// Store a person’s name with whitespace characters
let name6: string = "\t\n Haroon Taufiq \n\t";

// Print the name with whitespace
console.log('Name with whitespace: ' + name6);

// Strip the whitespace from the name
let strippedName: string = name6.trim();

// Print the name after stripping the whitespace
console.log(`Name after stripping whitespace: '${strippedName}'`);
