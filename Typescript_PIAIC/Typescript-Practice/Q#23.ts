// Declareing variable named 'car' and assign it the value 'subaru'
let car = 'subaru';

// Predict that 'car' is equal to 'subaru' and then check if it's true
console.log("Is car == 'subaru'? I predict True.");
console.log(car == 'subaru' ? true : false);

// Predict that 'car' is equal to 'audi' and then check if it's false
console.log("Is car == 'audi'? I predict False.");
console.log(car == 'audi' ? true : false);

// Predict that 'car' is not equal to 'audi' and then check if it's true
console.log("Is car != 'audi'? I predict True.");
console.log(car != 'audi' ? true : false);

// Predict that 'car' is not equal to 'subaru' and then check if it's false
console.log("Is car != 'subaru'? I predict False.");
console.log(car != 'subaru' ? true : false);

// Predict that 'car' converted to uppercase is equal to 'SUBARU' and then check if it's true
console.log("Is car.toUpperCase() == 'SUBARU'? I predict True.");
console.log(car.toUpperCase() == 'SUBARU' ? true : false);

// Predict that 'car' converted to uppercase is equal to 'SUBARU' converted to lowercase and then check if it's false
console.log("Is car.toUpperCase() == 'SUBARU'.toLowerCase()? I predict False.");
console.log(car.toUpperCase() == 'SUBARU'.toLowerCase() ? true : false);

// Predict that the length of 'car' is greater than 3 and then check if it's true
console.log("Is car.length > 3? I predict True.");
console.log(car.length > 3 ? true : false);

// Predict that the length of 'car' is less than 3 and then check if it's false
console.log("Is car.length < 3? I predict False.");
console.log(car.length < 3 ? true : false);

// Predict that the length of 'car' is equal to 6 and then check if it's true
console.log("Is car.length == 6? I predict True.");
console.log(car.length == 6 ? true : false);

// Predict that the length of 'car' is equal to 7 and then check if it's false
console.log(car.length == 7 ? true : false);