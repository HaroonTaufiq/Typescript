// Define an array of vehicles
const vehicles: string[] = ['Lamborghini', 'Tesla Model S', 'Ferrari', 'Ford Mustang'];

// Loop through the array and print a statement for each vehicle
for (const vehicle of vehicles) {
  console.log(`I would like to own a ${vehicle}.`);
}
